import { styled } from '@mui/material/styles'
import Image from 'next/image'

const CustomeImageStyled = styled(Image)(({ theme }) => ({
  
}))

export default function LogoAnimate() {

  return (
    <CustomeImageStyled />
  )
}